akka-http-server {
  listen-hostname = localhost
  connection-context = {
    enabled-cipher-suites = ["TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384", "TLS_DHE_RSA_WITH_AES_128_GCM_SHA256"]
    enabled-protocols = ["TLSv1.2"]
    client-auth = Need
  }
}